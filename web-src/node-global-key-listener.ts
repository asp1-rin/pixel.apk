// Stub for the type-only `import { IGlobalKey } from "node-global-key-listener"`
// in main.ts. The phone has no global keyboard hook; this satisfies the
// bundler. The value is never used at runtime.
export type IGlobalKey = string;
export default {};
